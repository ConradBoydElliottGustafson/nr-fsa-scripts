# This is a basic workflow to help you get started with Actions

name: zipscripts

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  # Identify the list of folders that need to be packaged into artifacts
  setup:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v2
      
      - name: List the directories in scripts
        id: set-matrix
        run: |
          declare -a dirs
          for d in scripts/*/ ; do
            echo "$d"
            test_array+=($d)
          done
          echo "::set-output name=matrix::[${test_array[@]}]"
          
  build:
    needs: [ setup ]
    runs-on: ubuntu-latest
    strategy:
      matrix: 
        dir: ${{fromJSON(needs.setup.outputs.matrix)}}
    steps:
      - run: |
          echo "${{ matrix.dir }}"
  


          
      
