# This is a basic workflow to help you get started with Actions

name: zipscripts

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  # Identify the list of folders that need to be packaged into artifacts
  setup:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: echo "::set-output name=matrix::[\"change1\", \"change2\"]"

  build:
    needs: [ setup ]
    runs-on: ubuntu-latest
    strategy:
      matrix: 
        dir: ${{fromJSON(needs.setup.outputs.matrix)}}
    steps:
      - run: |
          echo "${{ matrix.dir }}"
  


          
      
